<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="mostrarFecha" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <!-- Fondo negro -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- Verde Spotify para selección y botones -->
        <item name="colorAccent">@color/spotify_green</item>
        <item name="colorControlActivated">@color/spotify_green</item>
    </style>
</resources>